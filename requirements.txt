# Claude Code Launcher - 依赖包列表
# 为 macOS 和 Windows 平台提供跨平台支持

# 终端颜色输出支持
colorama>=0.4.6

# 系统和进程管理
psutil>=5.9.0

# 剪贴板操作（用于 Git 提交整理功能）
pyperclip>=1.8.2

# 开发和打包工具（可选）
# pyinstaller>=5.0  # 用于打包成可执行文件

# 注意事项：
# 1. termios 和 tty 是 Unix/Linux/macOS 系统的标准库，无需安装
# 2. msvcrt 是 Windows 系统的标准库，无需安装
# 3. 其他如 os, sys, json, subprocess 等都是 Python 标准库

# 安装方法：
# pip install -r requirements.txt

# 或使用国内镜像加速：
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
